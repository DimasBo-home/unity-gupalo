using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class patron : MonoBehaviour {

    [SerializeField]
    private float speed = 3;
    
	void FixedUpdate () {
//        transform.position = transform.position + new Vector3(speed * Time.deltaTime, transform.position.y, 0);
    }
}
